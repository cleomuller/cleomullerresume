variable "s3_bucket_names" {
  type    = list(any)
  default = ["the-cloud-resume-challenge-bucket"]
}

# Setting variable for subdomain hostname
variable "subdomain" {
  type    = string
  default = "www.stephaniechester.live"
}

variable "domain_name_root" {
  type    = string
  default = "stephaniechester.live"
}